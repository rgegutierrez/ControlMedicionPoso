<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:ControlMedicionPoso"
       x:Class="ControlMedicionPoso.AppShell">

    <FlyoutItem Title="Mediciones">
        <ShellContent ContentTemplate="{DataTemplate local:MedicionesPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Dashboard">
        <ShellContent ContentTemplate="{DataTemplate local:DashboardPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Ingreso Medición">
        <ShellContent ContentTemplate="{DataTemplate local:IngresoMedicionPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Carga Excel" Route="cargaexcel">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

</Shell>

<!--<Shell
    x:Class="ControlMedicionPoso.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ControlMedicionPoso"
    Shell.FlyoutBehavior="Disabled"
    Title="ControlMedicionPoso">

    <ShellContent
        Title="Control Mediciones Poso"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

</Shell>-->

